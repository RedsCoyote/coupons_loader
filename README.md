# Coupons loader for junglefox.me

Use 

<code>
cp ../phing_properties.yml build/phing_properties.yml
</code>

<code>
php phing.phar -f build/phing.xml -propertyfile build/phing_properties.yml
</code>

to install/update this application
